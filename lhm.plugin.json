{
  "identifier": "nilyo",
  "name": "Nilyo",
  "version": "1.5.2",
  "author": "Unipile SAS",
  "authorUrl": "https://github.com/nilyo-com",
  "category": "productivity",
  "cloudEndpoint": "https://nilyo.com/mcp",
  "homepage": "https://nilyo.com",
  "icon": "https://raw.githubusercontent.com/nilyo-com/nilyo-mcp/main/assets/logo-flat.png",
  "description": "Use your own LinkedIn, WhatsApp, Instagram, Telegram, Email (Gmail, Outlook, IMAP) and Calendar accounts from any agent \u2014 search, read, message and publish on your own accounts through a remote MCP with OAuth, exact ID resolution and human-like pacing.",
  "tags": [
    "linkedin",
    "whatsapp",
    "instagram",
    "telegram",
    "email",
    "gmail",
    "outlook",
    "imap",
    "calendar",
    "messaging",
    "sales",
    "prospecting",
    "recruiting",
    "crm",
    "inbox",
    "productivity",
    "communication",
    "oauth",
    "remote"
  ],
  "tools": [
    {
      "name": "agent_capability_guide",
      "description": "Return Nilyo's agent workflow recipes and chaining rules. Use when you need to know how to turn a LinkedIn URL/name into a provider user ID, how to find a chat before replying, how to resolve LinkedIn search parameter IDs, or how to combine several account providers.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workflow": {
            "description": "Optional workflow name; omit to return all recipes.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "list_connected_accounts",
      "description": "List the user's own accounts connected to Nilyo with provider, owner display name, identifier (LinkedIn public id, phone, email), connection status and reconnection hints. Use when the user asks what is connected, when several accounts of one provider exist and the user names one (\"with Julia's LinkedIn\"), or after a choose_account result. Nilyo never guesses between accounts: pick the entry matching what the user said and pass its unipile_account_id as account_id to the action tool.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "provider": {
            "description": "Optional provider filter such as linkedin, whatsapp, instagram, google, outlook or imap.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_profile",
      "description": "Get a LinkedIn person's full profile from a LinkedIn provider user ID, public identifier, or linkedin.com/in/... URL using the user's own account. This is the preferred resolver before an action when the user supplied only a profile URL. The result contains the stable user id needed by linkedin_start_conversation and linkedin_send_invitation. Optional sections can retrieve experience, skills, interests and other profile details.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id_or_url": {
            "type": "string",
            "description": "LinkedIn user ID (for example ACo...), public identifier, or full linkedin.com/in/... profile URL."
          },
          "variant": {
            "type": "string",
            "enum": [
              "linkedin_classic",
              "linkedin_sales_navigator",
              "linkedin_recruiter"
            ]
          },
          "with_sections": {
            "description": "Classic-only optional sections such as linkedin_experience, linkedin_skills or preview variants.",
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "linkedin_*",
                "linkedin_experience",
                "linkedin_education",
                "linkedin_languages",
                "linkedin_skills",
                "linkedin_certifications",
                "linkedin_volunteer_experience",
                "linkedin_projects",
                "linkedin_recommendations",
                "linkedin_recruiting_activity",
                "linkedin_interests",
                "linkedin_*_preview",
                "linkedin_experience_preview",
                "linkedin_education_preview",
                "linkedin_languages_preview",
                "linkedin_skills_preview",
                "linkedin_certifications_preview",
                "linkedin_volunteer_experience_preview",
                "linkedin_projects_preview",
                "linkedin_recommendations_preview",
                "linkedin_recruiting_activity_preview",
                "linkedin_interests_preview"
              ]
            }
          }
        },
        "required": [
          "user_id_or_url"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_search_people",
      "description": "Search LinkedIn Classic people with the user's own LinkedIn account. Use to find a person/provider user ID from a name, title, employer, location, industry, keywords or network distance. When the next requested action is message/invitation, take the chosen result's id and pass it to the action tool; never pass a profile URL directly to an action requiring user_id. Some filters require IDs; resolve them first with linkedin_get_search_parameters.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "keywords": {
            "type": "string"
          },
          "current_company": {
            "description": "LinkedIn company IDs, not company names, when the provider requires IDs.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "location": {
            "description": "LinkedIn location IDs; call linkedin_get_search_parameters when starting from a human location name.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "industry": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "network_distance": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "cursor": {
            "type": "string"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_sales_navigator_search_people",
      "description": "Search LinkedIn Sales Navigator leads using the user's own Sales Navigator-capable LinkedIn account. Use for advanced prospecting intent such as lead lists, seniority, years of experience, current/past company, geography, function and saved-search criteria. Resolve human filter names to LinkedIn IDs with linkedin_get_search_parameters(product='sales_navigator') when necessary.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_recruiter_search_people",
      "description": "Search LinkedIn Recruiter candidates using the user's Recruiter-capable account. Use for candidate sourcing, recruiting projects/talent pools and Recruiter-specific filters. Resolve filter IDs with linkedin_get_search_parameters(product='recruiter') when necessary.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_search_from_url",
      "description": "Run a native LinkedIn search from a LinkedIn search URL. Use when the user pastes a linkedin.com/search/results/... URL, Sales Navigator search URL or Recruiter search URL and wants the same filters/results available to the agent.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          },
          "product": {
            "default": "classic",
            "type": "string",
            "enum": [
              "classic",
              "sales_navigator",
              "recruiter"
            ]
          }
        },
        "required": [
          "url",
          "product"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_search_parameters",
      "description": "Resolve human LinkedIn filter values such as a location, company, industry, job function, relation, project, saved search or Sales Navigator list into the provider parameter IDs required by LinkedIn search endpoints. Typical sequence: call this with type + query, then pass the chosen returned id into linkedin_search_people / Sales Navigator / Recruiter search.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "LinkedIn parameter type, e.g. LOCATION, COMPANY, INDUSTRY, RELATION, JOB_FUNCTION, PROJECT, SAVED_SEARCH, LEAD_LIST."
          },
          "query": {
            "description": "Human text to resolve, such as Paris, Stripe or Software. Required for Classic search parameters; optional for Sales Navigator/Recruiter lists.",
            "type": "string"
          },
          "product": {
            "default": "classic",
            "type": "string",
            "enum": [
              "classic",
              "sales_navigator",
              "recruiter"
            ]
          }
        },
        "required": [
          "type",
          "product"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_search_companies",
      "description": "Search LinkedIn companies from the user's own account. Use to find a company ID/profile before looking for employees, checking network relationships or performing a people search scoped to that company.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "keywords": {
            "type": "string"
          },
          "location": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "industry": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "has_job_postings": {
            "type": "boolean"
          },
          "is_employing_relations": {
            "type": "boolean"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_get_company",
      "description": "Get a LinkedIn company profile by numeric/provider company ID. Use after linkedin_search_companies when full company data is needed.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "company_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn company ID returned by company search/resolution. Never pass a company name, URL or slug. LinkedIn company ID: Provider numeric/company ID. For mentions LinkedIn requires numeric company ID, not the company URL slug. Obtain with: linkedin_search_companies -> selected result.id; linkedin_get_company after resolving a company Never pass: linkedin.com/company/google URL, company slug such as google, company name."
          }
        },
        "required": [
          "company_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_search_posts",
      "description": "Search LinkedIn posts using the user's own account. Use to find a post before reading comments, checking reactions or replying. Returns post IDs required by comment/reaction tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "keywords": {
            "type": "string"
          },
          "sort_by": {
            "type": "string"
          },
          "date_posted": {
            "type": "string"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_search_jobs",
      "description": "Search LinkedIn jobs with the user's own account. Use for job discovery by keywords, title, company, location, seniority, employment status or workplace type.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "keywords": {
            "type": "string"
          },
          "job_title": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "primary_location": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "date_posted": {
            "type": "string",
            "enum": [
              "PAST_DAY",
              "PAST_WEEK",
              "PAST_MONTH"
            ]
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_list_my_connections",
      "description": "List/search the LinkedIn connections of the account owner. Use for questions such as 'who do I know at this company?', 'find Sarah in my network', introductions, relationship mapping and connection-aware prospecting. This reads the user's personal network rather than a company page.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "search": {
            "type": "string"
          },
          "cursor": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_user_relations",
      "description": "List a LinkedIn user's visible relations by provider user ID. Use when network graph context is requested for a known profile.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Stable LinkedIn system user ID returned by a profile/person resolver. Never pass a name or linkedin.com/in/... URL. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          },
          "search": {
            "type": "string"
          },
          "cursor": {
            "type": "string"
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_followers",
      "description": "List followers of the account owner or a known LinkedIn user. Use user_id='me' for the connected account owner.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "default": "me",
            "description": "Use 'me' for the connected account owner, otherwise an exact stable LinkedIn system user ID returned by a profile/person resolver. Never pass a name or profile URL. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID.",
            "type": "string",
            "minLength": 1
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_following",
      "description": "List LinkedIn users followed by the connected account owner. Prefer user_id='me'; provider availability may restrict following data for other users.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "default": "me",
            "description": "Use 'me' for the connected account owner, otherwise an exact stable LinkedIn system user ID returned by a profile/person resolver. Never pass a name or profile URL. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID.",
            "type": "string",
            "minLength": 1
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_invitations",
      "description": "List pending LinkedIn connection/relation requests sent or received by the account owner. Use before accepting/refusing a received invitation or withdrawing a sent invitation so you have the required request_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "type": {
            "type": "string",
            "enum": [
              "sent",
              "received"
            ]
          }
        },
        "required": [
          "type"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_send_invitation",
      "description": "Send a LinkedIn connection/relation request from the user's own account. Requires a provider user_id, not a profile URL. If the user supplied a URL or name, FIRST call linkedin_get_profile or linkedin_search_people and use the returned id. Include a note only when explicitly requested or clearly provided by the user.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "description": "Stable LinkedIn provider user ID obtained from linkedin_get_profile/search_people."
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_accept_invitation",
      "description": "Accept a received LinkedIn connection request. Requires request_id from linkedin_list_invitations(type='received').",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "request_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact pending invitation request ID returned by linkedin_list_invitations. Never pass the other person's user ID. Relation request ID: ID of a pending sent/received invitation request. Obtain with: linkedin_list_invitations(type=received|sent) -> request.id Never pass: LinkedIn user ID, profile URL."
          }
        },
        "required": [
          "request_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_cancel_or_refuse_invitation",
      "description": "Withdraw a sent LinkedIn invitation or refuse a received invitation. First list the relevant invitations to obtain request_id unless it is already known.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "request_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact pending invitation request ID returned by linkedin_list_invitations. Never pass the other person's user ID. Relation request ID: ID of a pending sent/received invitation request. Obtain with: linkedin_list_invitations(type=received|sent) -> request.id Never pass: LinkedIn user ID, profile URL."
          }
        },
        "required": [
          "request_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_user_posts",
      "description": "List posts authored by the connected LinkedIn user or a known provider user ID. Use to locate a recent post and obtain its post_id before reading comments/reactions or replying.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "default": "me",
            "description": "Use 'me' for the connected account owner, otherwise an exact stable LinkedIn system user ID returned by a profile/person resolver. Never pass a name or profile URL. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID.",
            "type": "string",
            "minLength": 1
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_post",
      "description": "Get a LinkedIn post by Nilyo/Unipile post_id. Use when you already know a post_id and need full post context.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID returned by post search/list/get. Never pass post text, author ID or URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_create_post",
      "description": "Publish a LinkedIn post from the user's own account, optionally on a managed company page using post_as. This is a public side effect; use only after the user has clearly approved the final content and target identity.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "text": {
            "type": "string"
          },
          "can_read": {
            "type": "string",
            "enum": [
              "anyone",
              "relations_only"
            ]
          },
          "can_comment": {
            "type": "string",
            "enum": [
              "anyone",
              "relations_only",
              "no_one"
            ]
          },
          "post_as": {
            "description": "Exact managed page/company identity returned by linkedin_list_managed_pages; omit to post as the connected person. Never pass a company name or URL.",
            "type": "string"
          },
          "quoted_post_id": {
            "description": "Exact provider post ID to quote, returned by post search/list/get. Never pass post text or a URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.",
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_post_comments",
      "description": "List comments on a LinkedIn post. Requires post_id from search/list/get post. Use before deciding which comments need a reply.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID returned by post search/list/get. Never pass post text, author ID or URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_comment_on_post",
      "description": "Publish a comment/reply on a LinkedIn post on behalf of the user. Requires post_id. Use after reading the post/comments and only when the user has asked to publish the final reply.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID returned by post search/list/get. Never pass post text, author ID or URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "text": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "post_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_post_reactions",
      "description": "List people/reactions on a LinkedIn post. Use for engagement analysis or to identify who reacted.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID returned by post search/list/get. Never pass post text, author ID or URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_react_to_post",
      "description": "Add a LinkedIn reaction to a post on behalf of the user. Use a supported LinkedIn reaction value and only when the user's intent to react is clear.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID returned by post search/list/get. Never pass post text, author ID or URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "reaction": {
            "type": "string",
            "enum": [
              "linkedin_like",
              "linkedin_celebrate",
              "linkedin_support",
              "linkedin_love",
              "linkedin_insightful",
              "linkedin_funny"
            ],
            "description": "LinkedIn reaction type (a plain 'like' or an emoji is rejected by LinkedIn)."
          }
        },
        "required": [
          "post_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_managed_company_pages",
      "description": "List LinkedIn company pages managed by the connected user. Use before publishing as a company page so you can resolve the required post_as company ID.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_inmail_credits",
      "description": "Get LinkedIn InMail credit information for the connected account. Use when the user asks whether an InMail can be sent or how many credits remain.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_conversations",
      "description": "List LinkedIn private conversations. By default only the Classic primary inbox (CLASSIC_PRIMARY); pass inbox_ids from linkedin_list_inboxes to read other inboxes (archived, spam, Sales Navigator, Recruiter, company pages). Inboxes are read one after another to respect LinkedIn rate limits; unreadable ones are reported with `unavailable` instead of failing. Preserve inbox_id and chat.id. Use before linkedin_read_conversation when chat_id is unknown.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "inbox_ids": {
            "description": "Exact inbox IDs from linkedin_list_inboxes (e.g. CLASSIC_PRIMARY, SALES_NAVIGATOR_PRIMARY). Default: CLASSIC_PRIMARY only.",
            "maxItems": 6,
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_read_conversation",
      "description": "Read a LinkedIn private conversation plus recent messages. Use before replying so the agent understands prior context, commitments and tone.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_send_message",
      "description": "Send a message in an EXISTING LinkedIn chat. Requires chat_id, normally obtained from linkedin_list_conversations. Use only when the user has clearly asked to send/approved the final text.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "text": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "chat_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_start_conversation",
      "description": "Start a new LinkedIn conversation and send a message. Requires the stable LinkedIn provider user_id. IMPORTANT: if the user gives a LinkedIn URL, name or search criteria, first call linkedin_get_profile or linkedin_search_people, extract the returned user id, then call this tool. Do not put a LinkedIn URL into linkedin_user_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "linkedin_user_id": {
            "type": "string",
            "description": "Stable provider user id resolved by linkedin_get_profile/search_people; not a linkedin.com URL."
          },
          "text": {
            "type": "string",
            "minLength": 1
          },
          "inbox_id": {
            "description": "LinkedIn inbox to start from (default CLASSIC_PRIMARY). Use a Sales Navigator/Recruiter inbox ID from linkedin_list_inboxes for InMail-style messages.",
            "type": "string"
          }
        },
        "required": [
          "linkedin_user_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_react_to_message",
      "description": "Add a reaction to a specific LinkedIn message. Requires chat_id and message_id, normally discovered by reading the conversation.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the selected chat. Keep it paired with chat_id. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          },
          "reaction": {
            "type": "string"
          }
        },
        "required": [
          "chat_id",
          "message_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_list_conversations",
      "description": "List WhatsApp chats from the user's own connected WhatsApp account. Use to find a chat_id before reading or sending when the user identifies a conversation by person/name rather than ID.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_read_conversation",
      "description": "Read a WhatsApp chat and recent message history. Use for context, summaries, follow-up detection and cross-channel workflows before sending a reply.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_send_message",
      "description": "Send a WhatsApp message in an existing chat from the user's own account. If chat_id is unknown, call whatsapp_list_conversations first. Use only after the final message is explicitly requested/approved.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "text": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "chat_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_list_conversations",
      "description": "List Instagram DM conversations from the user's own account. Use to locate a chat before reading/replying.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_read_conversation",
      "description": "Read an Instagram DM conversation and recent messages for context, lead qualification or follow-up workflows.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_send_message",
      "description": "Send an Instagram DM in an existing conversation from the user's own account. Locate chat_id first if necessary and only send after clear user approval.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID returned by conversation/inbox listing. Never pass a person name or provider user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "text": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "chat_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_list_messages",
      "description": "List email messages from the user's connected Gmail, Microsoft/Outlook OR generic IMAP mailbox. This is especially useful for IMAP mailboxes not covered by native agent connectors, and for cross-channel workflows that combine email context with LinkedIn/WhatsApp/Instagram. Use to find email_id before opening a message. For totals use email_list_folders (folder.total_count); IMAP pages are capped at 50 and listed live, keep limit small (20).",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "cursor": {
            "type": "string"
          },
          "folder_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact folder/label ID returned by email_list_folders/email_resolve_folder. Never pass a human folder name unless the provider returned it as the ID. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required."
          },
          "after": {
            "description": "ISO 8601 UTC datetime; only emails after it (exclusive).",
            "type": "string"
          },
          "before": {
            "description": "ISO 8601 UTC datetime; only emails before it (exclusive).",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "email_read_message",
      "description": "Read a specific email including body/headers from Gmail, Microsoft/Outlook or IMAP. Use after email_list_messages and before a contextual reply or cross-channel decision.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "email_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider email ID returned by an email list/resolve/read result. Never pass the subject or sender. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP."
          }
        },
        "required": [
          "email_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_send",
      "description": "Send or reply to an email from the user's connected Gmail, Microsoft/Outlook or IMAP account. Use reply_to_message_id for a true reply when available. Only send after the user has clearly requested/approved the final recipients and content.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "to": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "cc": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "bcc": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "subject": {
            "type": "string"
          },
          "plain_text": {
            "type": "string"
          },
          "html": {
            "type": "string"
          },
          "reply_to_message_id": {
            "description": "Provider reply reference from the source email. Gmail/Outlook normally use provider email ID; IMAP may require its RFC822 Message-ID. Email reply reference: For Gmail/Outlook normally use the provider email id. For generic IMAP, reply_to_message_id may be the RFC822 Message-ID returned in the email data. Obtain with: email_read_message -> inspect id and provider/RFC Message-ID fields Never pass: subject, sender email address.",
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "to"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_comment_replies",
      "description": "List replies under one exact LinkedIn comment. Chain: resolve post -> post.id -> list comments -> comment.id -> replies. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact post ID from post search/list. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID from comments of the resolved post. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "post_id",
          "comment_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_reply_to_comment",
      "description": "Reply DIRECTLY to one exact LinkedIn comment, not create a new top-level comment. Required chain from human reference: resolve post -> post.id -> list comments -> comment.id -> optionally read replies -> reply. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact post ID from post search/list. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID from comments of the resolved post. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "Final reply text explicitly requested/approved by the user."
          }
        },
        "required": [
          "post_id",
          "comment_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_list_contacts",
      "description": "List WhatsApp contacts so a human name/phone can be resolved to an exact provider user identity before starting a new chat. Reuse the returned provider user ID; do not pass display name as whatsapp_user_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "cursor": {
            "description": "Opaque pagination cursor returned by previous call; never invent.",
            "type": "string"
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_start_conversation",
      "description": "Start a new 1-to-1 WhatsApp chat and send the first message. Resolve/verify recipient first with whatsapp_list_contacts or WhatsApp profile/number lookup. whatsapp_user_id must be provider identity, never a contact display name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "whatsapp_user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact WhatsApp provider user ID returned by contact/profile/number resolution; not a human name."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "First message text explicitly requested/approved by user."
          }
        },
        "required": [
          "whatsapp_user_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_list_folders",
      "description": "List Gmail labels, Outlook folders or IMAP folders. Returned folder.id is required by folder-specific operations. For IMAP this is normally the first step before listing messages. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "cursor": {
            "description": "Opaque cursor returned by prior folder listing; never invent.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "email_get_folder",
      "description": "Get one exact Gmail/Outlook/IMAP folder. Resolve folder name -> folder.id first. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "folder_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider folder/label ID from email_list_folders/email_resolve_folder. Human folder names are not IDs. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required."
          }
        },
        "required": [
          "folder_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_list_folder_messages",
      "description": "List messages inside one exact folder. Especially for IMAP: list folders -> select folder.id -> list messages -> email.id -> read/action. Dates: after/before are ISO 8601 UTC datetimes (YYYY-MM-DDTHH:MM:SS.sssZ); for \"today\" use after=start of the user's day. Counting: an empty page is not \"zero emails\" unless no filter was applied; for totals use folder.total_count/unread_count from email_list_folders instead of listing. IMAP pages are capped at 50 live messages; keep limit small (20) to avoid runtime timeouts. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.\nEmail provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "folder_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider folder/label ID from email_list_folders/email_resolve_folder. Human folder names are not IDs. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "cursor": {
            "description": "Opaque pagination cursor returned by previous message listing.",
            "type": "string"
          },
          "before": {
            "type": "string"
          },
          "after": {
            "type": "string"
          },
          "from": {
            "type": "string"
          },
          "to": {
            "type": "string"
          },
          "any_email": {
            "type": "string"
          }
        },
        "required": [
          "folder_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_get_thread",
      "description": "Retrieve full email thread by exact thread_id. Obtain thread_id from email list/read response; do not substitute subject/email_id. Email thread ID: Exact thread/conversation id returned by the email provider when available. Obtain with: email list/read result -> thread_id Never pass: email_id unless equal according to returned data.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "thread_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact thread ID returned by the email provider. Do not substitute email_id unless returned data says they are equal. Email thread ID: Exact thread/conversation id returned by the email provider when available. Obtain with: email list/read result -> thread_id Never pass: email_id unless equal according to returned data."
          }
        },
        "required": [
          "thread_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_mark_read",
      "description": "Mark one exact email read. Resolve human email reference -> email_id first. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "email_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider email ID from email list/read/resolve. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP."
          }
        },
        "required": [
          "email_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_mark_unread",
      "description": "Mark one exact email unread. Resolve email_id first. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "email_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider email ID from email list/read/resolve. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP."
          }
        },
        "required": [
          "email_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_move_or_label",
      "description": "Move/label one exact email. The result is the email after the change: on IMAP its id CHANGES (folder+UID), so reuse result.id for any follow-up. Resolve email_id independently, then resolve every human folder name to folder.id. Never put 'Inbox'/'Archive' strings in folders_ids unless the provider actually returned those strings as IDs. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.\nMailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "email_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider email ID from email list/read/resolve. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP."
          },
          "folders_ids": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "description": "Exact provider folder/label ID from email_list_folders/email_resolve_folder. Human folder names are not IDs. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required."
            }
          },
          "specifics": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "email_id",
          "folders_ids"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_trash",
      "description": "Trash one exact email. Resolve email_id first; destructive mailbox action. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "email_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider email ID from email list/read/resolve. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP."
          }
        },
        "required": [
          "email_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_create_folder",
      "description": "Create a mailbox folder/label. parent_id, when supplied, must be an exact existing folder ID resolved from email_list_folders; it is not a parent folder display name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "parent_id": {
            "description": "Exact existing parent folder ID. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": {}
      }
    },
    {
      "name": "email_update_folder",
      "description": "Rename/move one existing folder. Resolve folder_id first; parent_id is also an exact folder ID when supplied. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "folder_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider folder/label ID from email_list_folders/email_resolve_folder. Human folder names are not IDs. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required."
          },
          "name": {
            "type": "string"
          },
          "parent_id": {
            "description": "Exact parent folder ID from folder listing. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
            "type": "string"
          }
        },
        "required": [
          "folder_id"
        ],
        "additionalProperties": {}
      }
    },
    {
      "name": "email_delete_folder",
      "description": "Delete one exact mailbox folder/label. Resolve folder_id first and verify target; destructive. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "folder_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider folder/label ID from email_list_folders/email_resolve_folder. Human folder names are not IDs. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required."
          }
        },
        "required": [
          "folder_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_list_drafts",
      "description": "List email drafts. Returned draft.id is the draft_id for get/update/send/delete. draft_id is not email_id or subject.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "cursor": {
            "description": "Opaque pagination cursor returned by previous call.",
            "type": "string"
          },
          "any_email": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "email_get_draft",
      "description": "Get one exact draft. draft_id must come from email_list_drafts or email_create_draft; never use subject/email_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "draft_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact draft ID returned by email_list_drafts/email_create_draft; never use subject or email_id as draft_id."
          }
        },
        "required": [
          "draft_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_create_draft",
      "description": "Create an email draft without sending. If replying, first read the source email and use the provider-appropriate reply_to_message_id (IMAP may use RFC822 Message-ID).",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "to": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
                  "description": "Recipient email address, not a contact/provider ID."
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "cc": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
                  "description": "Recipient email address, not a contact/provider ID."
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "bcc": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
                  "description": "Recipient email address, not a contact/provider ID."
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "subject": {
            "type": "string"
          },
          "plain_text": {
            "type": "string"
          },
          "html": {
            "type": "string"
          },
          "reply_to_message_id": {
            "description": "Reply reference. Gmail/Outlook normally use provider email ID; IMAP may require RFC822 Message-ID from the read email. Email reply reference: For Gmail/Outlook normally use the provider email id. For generic IMAP, reply_to_message_id may be the RFC822 Message-ID returned in the email data. Obtain with: email_read_message -> inspect id and provider/RFC Message-ID fields Never pass: subject, sender email address.",
            "type": "string"
          },
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "required": [
          "to"
        ],
        "additionalProperties": {}
      }
    },
    {
      "name": "email_update_draft",
      "description": "Update an exact existing draft. On IMAP the updated draft gets a NEW draft.id (returned in the result); use it for email_send_draft.  without sending. Resolve/read draft first when preserving recipients/content matters.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "to": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
                  "description": "Recipient email address, not a contact/provider ID."
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "cc": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
                  "description": "Recipient email address, not a contact/provider ID."
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "bcc": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
                  "description": "Recipient email address, not a contact/provider ID."
                },
                "display_name": {
                  "type": "string"
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "subject": {
            "type": "string"
          },
          "plain_text": {
            "type": "string"
          },
          "html": {
            "type": "string"
          },
          "reply_to_message_id": {
            "description": "Reply reference. Gmail/Outlook normally use provider email ID; IMAP may require RFC822 Message-ID from the read email. Email reply reference: For Gmail/Outlook normally use the provider email id. For generic IMAP, reply_to_message_id may be the RFC822 Message-ID returned in the email data. Obtain with: email_read_message -> inspect id and provider/RFC Message-ID fields Never pass: subject, sender email address.",
            "type": "string"
          },
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "draft_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact draft ID returned by email_list_drafts/email_create_draft; never use subject or email_id as draft_id."
          }
        },
        "required": [
          "draft_id"
        ],
        "additionalProperties": {}
      }
    },
    {
      "name": "email_send_draft",
      "description": "Send one exact existing draft. Resolve draft_id and inspect final recipients/content first when not already explicitly approved; this creates a real external send.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "draft_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact draft ID returned by email_list_drafts/email_create_draft; never use subject or email_id as draft_id."
          }
        },
        "required": [
          "draft_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_delete_draft",
      "description": "Discard one exact draft. draft_id comes from draft list/create; destructive.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "draft_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact draft ID returned by email_list_drafts/email_create_draft; never use subject or email_id as draft_id."
          }
        },
        "required": [
          "draft_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_list_contacts",
      "description": "List Gmail/Microsoft contacts to resolve a human recipient name to an email address before composing. Generic IMAP normally has no provider contact directory; use mailbox context instead.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "cursor": {
            "description": "Opaque cursor returned by previous contact listing.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_contracts",
      "description": "List LinkedIn premium contracts available on the user's account. Returned contract.id is the ONLY value to pass as contract_id to linkedin_select_contract. contract_id is distinct from inbox_id. LinkedIn premium contract ID: Exact Sales Navigator/Recruiter contract id. Obtain with: linkedin_list_contracts -> contract.id Never pass: inbox_id, product name.\nInbox ID: Provider inbox/product inbox identifier. LinkedIn Classic may use CLASSIC; premium products expose their own inbox IDs. Obtain with: linkedin_list_inboxes -> inbox.id Never pass: contract_id unless returned as the inbox id, guessing SALES_NAVIGATOR or RECRUITER.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_select_contract",
      "description": "Select a LinkedIn Sales Navigator/Recruiter premium contract. First call linkedin_list_contracts and reuse exact contract.id. Do not pass product name or inbox_id. LinkedIn premium contract ID: Exact Sales Navigator/Recruiter contract id. Obtain with: linkedin_list_contracts -> contract.id Never pass: inbox_id, product name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "contract_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact premium contract ID returned by linkedin_list_contracts -> contract.id. LinkedIn premium contract ID: Exact Sales Navigator/Recruiter contract id. Obtain with: linkedin_list_contracts -> contract.id Never pass: inbox_id, product name."
          }
        },
        "required": [
          "contract_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_job_postings",
      "description": "List LinkedIn job postings OWNED by the user's account. Use this to resolve job_posting_id for management/budget operations. This differs from linkedin_search_jobs, which discovers jobs across LinkedIn.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum postings to return.",
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "cursor": {
            "description": "Pagination cursor returned by the previous list call; never invent it.",
            "type": "string"
          },
          "status": {
            "description": "Provider-supported posting status filter.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_job_posting",
      "description": "Get one job posting owned by the user's LinkedIn account. job_posting_id MUST come from linkedin_list_job_postings, not a title/company or an unrelated discovery result. LinkedIn job ID: Exact LinkedIn job/job-posting ID returned by the corresponding search/list endpoint. Obtain with: linkedin_search_jobs -> result.id for discovery; linkedin_list_job_postings -> result.id for jobs owned by the account Never pass: job title, company ID.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "job_posting_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact owned job posting ID returned by linkedin_list_job_postings -> result.id. LinkedIn job ID: Exact LinkedIn job/job-posting ID returned by the corresponding search/list endpoint. Obtain with: linkedin_search_jobs -> result.id for discovery; linkedin_list_job_postings -> result.id for jobs owned by the account Never pass: job title, company ID."
          }
        },
        "required": [
          "job_posting_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_update_post",
      "description": "Edit an existing supported social post. Resolve the exact post first, then reuse post.id. Never infer an ID from post text or author. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "text": {
            "description": "Replacement/updated post text requested by the user.",
            "type": "string"
          },
          "can_comment": {
            "type": "string",
            "enum": [
              "anyone",
              "relations_only",
              "no_one"
            ]
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_delete_post",
      "description": "Delete an exact supported social post. Resolve/verify post_id first. Destructive: explicit deletion intent required. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_update_comment",
      "description": "Edit an existing comment. Required chain when starting from human text: resolve post -> post.id -> list comments -> comment.id -> edit. post_id and comment_id are different ID namespaces. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID belonging to the already resolved post_id. Obtain by listing comments on that post; never pass comment text or post_id here. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "Final replacement comment text explicitly requested/approved by the user."
          }
        },
        "required": [
          "post_id",
          "comment_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_delete_comment",
      "description": "Delete one exact comment. Resolve post_id then comment_id; never use comment text as ID. Destructive. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID belonging to the already resolved post_id. Obtain by listing comments on that post; never pass comment text or post_id here. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          }
        },
        "required": [
          "post_id",
          "comment_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_list_comment_reactions",
      "description": "List detailed reactions on an exact comment. First resolve post_id, then list comments and select comment.id. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID belonging to the already resolved post_id. Obtain by listing comments on that post; never pass comment text or post_id here. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          },
          "offset": {
            "description": "Pagination offset returned/used by this reaction listing.",
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "post_id",
          "comment_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_react_to_comment",
      "description": "React to one exact comment. Resolve post -> comment before acting. Do not confuse post_id and comment_id. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID belonging to the already resolved post_id. Obtain by listing comments on that post; never pass comment text or post_id here. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          },
          "reaction": {
            "type": "string",
            "minLength": 1,
            "description": "Provider-supported reaction value; use the requested semantic reaction and do not invent unsupported values."
          }
        },
        "required": [
          "post_id",
          "comment_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_remove_comment_reaction",
      "description": "Remove the user's reaction from one exact comment. Resolve post_id/comment_id first. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "comment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact comment ID belonging to the already resolved post_id. Obtain by listing comments on that post; never pass comment text or post_id here. Comment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id."
          },
          "reaction": {
            "type": "string",
            "minLength": 1,
            "description": "The reaction to remove: emoji or provider reaction type that was added."
          }
        },
        "required": [
          "post_id",
          "comment_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "social_remove_post_reaction",
      "description": "Remove the user's reaction from one exact post. Resolve post_id first; never use post URL/text unless another resolver explicitly converts it. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID. Resolve/list the post first; never pass post text, author ID or a human description. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "reaction": {
            "type": "string",
            "minLength": 1,
            "description": "The reaction to remove: the emoji for Instagram, or the LinkedIn reaction type that was added (e.g. LIKE)."
          }
        },
        "required": [
          "post_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_get",
      "description": "Get one exact message. BOTH chat_id and message_id are required because V2 message identity is contextual to its chat. If starting from a person/topic, resolve chat first, list messages, then reuse both IDs. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          }
        },
        "required": [
          "chat_id",
          "message_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_mark_read",
      "description": "Mark one exact message read. Resolve chat_id then message_id; do not pass person/user ID as chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          }
        },
        "required": [
          "chat_id",
          "message_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_edit",
      "description": "Edit one previously sent message where supported. Recommended chain: resolve chat -> list messages -> message_get -> edit. Keep exact chat_id + message_id pair. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "Final replacement message text explicitly requested/approved by the user."
          }
        },
        "required": [
          "chat_id",
          "message_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_delete",
      "description": "Delete one exact message. Resolve and verify chat/message first; destructive. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          }
        },
        "required": [
          "chat_id",
          "message_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_forward",
      "description": "Forward an existing message to another EXISTING chat. Resolve source chat + message and destination chat independently. target_chat_id is a chat ID, never recipient user_id/name. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          },
          "target_chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact destination chat ID from message_resolve_chat/list conversations. Never pass a person name/user_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id",
          "message_id",
          "target_chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_list_reactions",
      "description": "List detailed reactions on one exact message. Preserve its chat_id + message_id pair. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          }
        },
        "required": [
          "chat_id",
          "message_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_add_reaction",
      "description": "Add an emoji reaction to one exact WhatsApp, Instagram, Telegram or LinkedIn message (e.g. \ud83d\udc4d, \u2764\ufe0f). Resolve chat/message first; keep chat_id + message_id paired. Use message_remove_reaction to withdraw it. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          },
          "reaction": {
            "type": "string",
            "minLength": 1,
            "description": "Emoji to react with, e.g. \ud83d\udc4d."
          }
        },
        "required": [
          "chat_id",
          "message_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_remove_reaction",
      "description": "Remove the user's reaction from one exact message. Resolve chat/message first. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nMessage ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "message_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message ID returned inside the specified chat. Keep it paired with chat_id for all V2 message mutations/reactions. Message ID: Exact message id inside a specific chat. In V2 always keep chat_id together with message_id because providers may only guarantee uniqueness inside the chat. Obtain with: provider read/list conversation messages -> message.id Never pass: message text, chat_id, message ID without its chat context for mutation tools."
          },
          "reaction": {
            "type": "string",
            "minLength": 1,
            "description": "The emoji/reaction to remove (the same value that was added, e.g. \ud83d\udc4d); list reactions first when unknown."
          }
        },
        "required": [
          "chat_id",
          "message_id",
          "reaction"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "chat_list_participants",
      "description": "List members of an exact group/multi-party chat. Resolve chat_id first. Returned participant user IDs can be reused for membership operations; they are NOT chat IDs. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "chat_add_participant",
      "description": "Add an exact provider user to an exact group chat. Resolve chat_id independently from user_id. For LinkedIn user references, URL/name -> profile resolver -> profile.id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.\nLinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/person/contact resolver. Never pass a display name. For LinkedIn actions use the stable system user ID returned as profile.id, not a linkedin.com URL."
          }
        },
        "required": [
          "chat_id",
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "chat_remove_participant",
      "description": "Remove an exact provider user from an exact group chat. Inspect participants first when identity is ambiguous. chat_id and user_id are separate. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/person/contact resolver. Never pass a display name. For LinkedIn actions use the stable system user ID returned as profile.id, not a linkedin.com URL."
          }
        },
        "required": [
          "chat_id",
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "chat_update",
      "description": "Update mutable metadata of an exact chat. Resolve chat_id first; do not use person/user ID. Pass only fields explicitly requested. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": {}
      }
    },
    {
      "name": "chat_delete",
      "description": "Delete/remove one exact chat where supported. Resolve/verify chat_id first; destructive. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from a conversation/chat listing or resolver. A person name/user_id is NOT a chat_id. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_get_attachment",
      "description": "Retrieve one exact attachment from Gmail, Outlook/Microsoft or IMAP. Required chain: resolve/list email -> email_id -> read email -> attachment.id -> retrieve. Filename is NOT attachment_id. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.\nAttachment ID: Exact attachment id returned inside a specific email/message. Obtain with: read the parent email/message first -> attachment.id Never pass: filename, URL.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "email_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact email provider ID from email list/read/resolve tools. Do not pass subject or RFC Message-ID unless a specific reply field explicitly asks for RFC Message-ID. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP."
          },
          "attachment_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact attachment ID from the already-read parent email. Never use filename or URL as attachment_id. Attachment ID: Exact attachment id returned inside a specific email/message. Obtain with: read the parent email/message first -> attachment.id Never pass: filename, URL."
          }
        },
        "required": [
          "email_id",
          "attachment_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_my_profile",
      "description": "Get the profile of the owner of the connected LinkedIn account. Use for 'my LinkedIn profile', identity/context, or before actions that need the account owner's provider user ID.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_visit_profile",
      "description": "Report a LinkedIn profile visit using notify_visit_token returned by the resolved profile. The token is provider-issued and must not be replaced with user_id, profile URL or public identifier.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "notify_visit_token": {
            "type": "string",
            "minLength": 1,
            "description": "Exact notify_visit_token returned by linkedin_get_profile_from_url/linkedin_get_profile for the profile being visited; never invent it."
          }
        },
        "required": [
          "notify_visit_token"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_update_my_profile",
      "description": "Update supported fields of the user's own LinkedIn profile. Only pass fields explicitly requested by the user; provider support varies by field.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "headline": {
            "type": "string"
          },
          "first_name": {
            "type": "string"
          },
          "last_name": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "summary": {
            "type": "string"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "linkedin_remove_connection",
      "description": "Remove an existing LinkedIn relation/connection. Resolve the exact provider user_id first. This changes the user's network and requires explicit intent.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_follow_user",
      "description": "Follow a LinkedIn user from the connected personal account. Resolve the exact provider user_id first.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_unfollow_user",
      "description": "Unfollow a LinkedIn user from the connected account. Resolve the exact provider user_id first.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_endorse_skill",
      "description": "Endorse one skill on a LinkedIn relation. Resolve member_id first, fetch that profile with linkedin_skills, and reuse the exact returned endorsement skill_id; never pass a skill name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "member_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn member profile ID returned by the profile resolver; not a URL/name."
          },
          "skill_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact endorsement skill ID from the member profile's linkedin_skills section; not the display name."
          }
        },
        "required": [
          "member_id",
          "skill_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_job_posting_budget",
      "description": "Get budget/pricing information for a LinkedIn Classic job posting owned by the connected account. Resolve job_id with linkedin_list_job_postings first.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "job_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact Classic job ID returned by linkedin_list_job_postings; not title or URL."
          }
        },
        "required": [
          "job_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_inboxes",
      "description": "List LinkedIn messaging inboxes for Classic, Sales Navigator and Recruiter. Use before listing inbox chats or starting Sales Navigator/Recruiter conversations; premium products require the appropriate primary inbox.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_list_inbox_chats",
      "description": "List chats from a specific LinkedIn inbox. Prefer this over generic chat listing for LinkedIn V2 and premium Sales Navigator/Recruiter inboxes. Get inbox_id from linkedin_list_inboxes.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "inbox_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn inbox ID returned by linkedin_list_inboxes. Preserve Classic/Sales Navigator/Recruiter selection; never pass contract_id or product name. Inbox ID: Provider inbox/product inbox identifier. LinkedIn Classic may use CLASSIC; premium products expose their own inbox IDs. Obtain with: linkedin_list_inboxes -> inbox.id Never pass: contract_id unless returned as the inbox id, guessing SALES_NAVIGATOR or RECRUITER."
          },
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "is_unread": {
            "type": "boolean"
          },
          "before": {
            "type": "string"
          },
          "after": {
            "type": "string"
          }
        },
        "required": [
          "inbox_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_start_conversation_from_inbox",
      "description": "Start a LinkedIn Classic, Sales Navigator or Recruiter conversation from an explicit inbox. Resolve recipient user IDs first and obtain inbox_id with linkedin_list_inboxes. For Sales Navigator include the required subject in specifics; Recruiter may require subject and signature. Supports one-to-one or group recipients and attachments.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "inbox_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn inbox ID returned by linkedin_list_inboxes. Preserve Classic/Sales Navigator/Recruiter selection; never pass contract_id or product name. Inbox ID: Provider inbox/product inbox identifier. LinkedIn Classic may use CLASSIC; premium products expose their own inbox IDs. Obtain with: linkedin_list_inboxes -> inbox.id Never pass: contract_id unless returned as the inbox id, guessing SALES_NAVIGATOR or RECRUITER."
          },
          "users_ids": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1,
                "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
              },
              {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
                }
              }
            ],
            "description": "One exact provider user ID for a direct chat, or exact provider user IDs for a group; resolve every recipient first."
          },
          "text": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "attachments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "filename": {
                  "type": "string"
                },
                "content_type": {
                  "type": "string"
                },
                "content": {
                  "type": "string",
                  "description": "Base64 encoded file content."
                },
                "send_mode": {
                  "type": "string",
                  "enum": [
                    "file",
                    "native"
                  ]
                },
                "voice_note": {
                  "type": "boolean"
                }
              },
              "required": [
                "filename",
                "content_type",
                "content"
              ],
              "additionalProperties": false
            }
          },
          "specifics": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "inbox_id",
          "users_ids",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_find_chat_by_user",
      "description": "Find the existing one-to-one chat for a known LinkedIn, WhatsApp, Instagram or Telegram provider user ID. Use after resolving the user; user_id is not chat_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by a profile/contact resolver; never a name or profile URL."
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_list_chats",
      "description": "List chats for WhatsApp, Instagram or Telegram. Exposes V2 pagination and filters: type, before/after, archived and unread. LinkedIn requires inbox-specific listing.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "provider": {
            "type": "string",
            "enum": [
              "whatsapp",
              "instagram",
              "telegram"
            ]
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "type": {
            "type": "string",
            "enum": [
              "1to1",
              "group",
              "channel"
            ]
          },
          "before": {
            "type": "string"
          },
          "after": {
            "type": "string"
          },
          "is_archived": {
            "type": "boolean"
          },
          "is_unread": {
            "type": "boolean"
          }
        },
        "required": [
          "provider"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_list_messages",
      "description": "List messages in a known chat with full V2 filters/pagination. Message IDs are scoped to their chat: keep chat_id for any later get/edit/delete/forward/reaction action.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from the appropriate chat/inbox listing or resolver. Never pass a person name or user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "cursor": {
            "type": "string"
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "before": {
            "type": "string"
          },
          "after": {
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_send_message",
      "description": "Send a message in an existing LinkedIn, WhatsApp, Instagram or Telegram chat. Supports text, base64 file attachments and provider-specific options. Locate/read the chat first if chat_id is not already known. This is a real external send action.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from the appropriate chat/inbox listing or resolver. Never pass a person name or user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "text": {
            "type": "string"
          },
          "attachments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "filename": {
                  "type": "string"
                },
                "content_type": {
                  "type": "string"
                },
                "content": {
                  "type": "string",
                  "description": "Base64 encoded file content."
                },
                "send_mode": {
                  "type": "string",
                  "enum": [
                    "file",
                    "native"
                  ]
                },
                "voice_note": {
                  "type": "boolean"
                }
              },
              "required": [
                "filename",
                "content_type",
                "content"
              ],
              "additionalProperties": false
            }
          },
          "specifics": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "chat_id",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_start_chat",
      "description": "Start a new WhatsApp, Instagram or Telegram one-to-one/group chat by sending its first message. Resolve users_ids first. A string starts 1-to-1; an array requests a group. LinkedIn requires the inbox-specific start tool.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "provider": {
            "type": "string",
            "enum": [
              "whatsapp",
              "instagram",
              "telegram"
            ]
          },
          "users_ids": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1,
                "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
              },
              {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
                }
              }
            ],
            "description": "One exact provider user ID for a direct chat, or exact provider user IDs for a group; resolve every recipient first."
          },
          "text": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "attachments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "filename": {
                  "type": "string"
                },
                "content_type": {
                  "type": "string"
                },
                "content": {
                  "type": "string",
                  "description": "Base64 encoded file content."
                },
                "send_mode": {
                  "type": "string",
                  "enum": [
                    "file",
                    "native"
                  ]
                },
                "voice_note": {
                  "type": "boolean"
                }
              },
              "required": [
                "filename",
                "content_type",
                "content"
              ],
              "additionalProperties": false
            }
          },
          "specifics": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "provider",
          "users_ids",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_set_chat_state",
      "description": "Update supported chat state such as read/unread, archive/pin, label or mute metadata. Only send fields explicitly requested.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from the appropriate chat/inbox listing or resolver. Never pass a person name or user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "name": {
            "type": "string"
          },
          "read_status": {
            "description": "True marks the chat read; false marks it unread.",
            "type": "boolean"
          },
          "archive_status": {
            "type": "boolean"
          },
          "pin_status": {
            "type": "boolean"
          },
          "label": {
            "type": "string"
          },
          "muted_until": {
            "type": [
              "boolean",
              "string"
            ]
          }
        },
        "required": [
          "chat_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_set_presence",
      "description": "Set the connected messaging account presence where the provider supports it (LinkedIn Classic, WhatsApp, Telegram). Use only for an explicit presence request.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "presence": {
            "type": "string",
            "enum": [
              "online",
              "restricted_online",
              "offline",
              "away"
            ]
          }
        },
        "required": [
          "presence"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_set_composing",
      "description": "Publish a typing or voice-recording composing signal in a known chat where supported (LinkedIn Classic and WhatsApp). This is ephemeral provider state; use only when an agent workflow explicitly needs it.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat/conversation ID from the appropriate chat/inbox listing or resolver. Never pass a person name or user ID. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id."
          },
          "action": {
            "type": "string",
            "enum": [
              "typing",
              "recording"
            ]
          }
        },
        "required": [
          "chat_id",
          "action"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_is_number_registered",
      "description": "Check whether a phone number corresponds to a WhatsApp user/account before starting outreach or resolving a WhatsApp recipient. Pass the number in international form.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "phone_number": {
            "type": "string"
          }
        },
        "required": [
          "phone_number"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_get_profile",
      "description": "Retrieve a WhatsApp user profile by provider user ID or supported phone-number identifier. Use to resolve/verify the recipient before a new chat.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_get_my_profile",
      "description": "Get the profile of the owner of the connected Instagram account.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_get_profile",
      "description": "Get an Instagram user profile by provider user ID/username where supported. Use before follow/unfollow or identity-sensitive actions.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider user ID returned by the relevant profile/contact resolver; never a display name or profile URL. For LinkedIn, use the stable system ID. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID."
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_update_my_profile",
      "description": "Update supported fields on the user's own Instagram profile. Only pass fields explicitly requested; availability is provider-dependent.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "instagram_list_followers",
      "description": "List followers of the connected Instagram account or a known Instagram user where supported.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "default": "me",
            "description": "Use 'me' for the connected account owner, otherwise the exact provider user ID returned by a profile/contact resolver; never a display name or profile URL.",
            "type": "string",
            "minLength": 1
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "instagram_list_following",
      "description": "List Instagram accounts followed by the connected account/user where supported.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "user_id": {
            "default": "me",
            "description": "Use 'me' for the connected account owner, otherwise the exact provider user ID returned by a profile/contact resolver; never a display name or profile URL.",
            "type": "string",
            "minLength": 1
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          }
        },
        "required": [
          "user_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_list_calendars",
      "description": "List calendars for the connected Google or Microsoft account. Use the returned calendar.id before listing, creating or updating events; never pass a calendar display name as calendar_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_create_calendar",
      "description": "Create a calendar in the selected Google or Microsoft account. Use only when the user explicitly asks for a new calendar, not merely an event.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "description": {
            "type": "string"
          },
          "background_color": {
            "description": "Hexadecimal calendar color.",
            "type": "string"
          },
          "timezone": {
            "description": "IANA timezone, for example Europe/Paris.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_get_calendar",
      "description": "Get one calendar by the exact ID returned by calendar_list_calendars. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          }
        },
        "required": [
          "calendar_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_update_calendar",
      "description": "Update explicit fields on an existing calendar. Resolve calendar_id first and pass only changes requested by the user.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "description": {
            "type": "string"
          },
          "background_color": {
            "description": "Hexadecimal calendar color.",
            "type": "string"
          },
          "timezone": {
            "description": "IANA timezone, for example Europe/Paris.",
            "type": "string"
          }
        },
        "required": [
          "calendar_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_delete_calendar",
      "description": "Delete one exact calendar. This is destructive: resolve and confirm the calendar before deleting it; never infer calendar_id from its display name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          }
        },
        "required": [
          "calendar_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_list_events",
      "description": "List or search events inside one resolved calendar. Use this to obtain event.id before reading, updating or deleting an event.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "is_cancelled": {
            "type": "boolean"
          },
          "title": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "ical_uid": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "start": {
            "description": "ISO 8601 lower time bound.",
            "type": "string"
          },
          "end": {
            "description": "ISO 8601 upper time bound.",
            "type": "string"
          },
          "busy": {
            "type": "boolean"
          },
          "updated_before": {
            "type": "string"
          },
          "updated_after": {
            "type": "string"
          },
          "attendees": {
            "type": "string"
          },
          "event_type": {
            "type": "string"
          },
          "expand_recurring": {
            "type": "boolean"
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "calendar_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_create_event",
      "description": "Create an event in one resolved calendar. Require an explicit title, attendees array, start and end; preserve timezone and all-day semantics instead of guessing them.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "title": {
            "type": "string",
            "minLength": 1
          },
          "body": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "background_color": {
            "type": "string"
          },
          "transparency": {
            "type": "string",
            "enum": [
              "opaque",
              "transparent"
            ]
          },
          "visibility": {
            "type": "string",
            "enum": [
              "public",
              "private"
            ]
          },
          "conference": {
            "type": "object",
            "properties": {
              "provider": {
                "type": "string",
                "enum": [
                  "google_meet",
                  "zoom",
                  "skype",
                  "teams",
                  "unknown"
                ]
              },
              "conference_id": {
                "type": "string"
              },
              "url": {
                "type": "string",
                "format": "uri"
              }
            },
            "required": [
              "provider"
            ],
            "additionalProperties": false
          },
          "is_attendees_list_hidden": {
            "type": "boolean"
          },
          "attendees": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
                },
                "type": {
                  "type": "string",
                  "enum": [
                    "required",
                    "optional"
                  ]
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "recurrence": {
            "description": "RFC5545 RRULE, EXRULE, RDATE or EXDATE lines.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "start": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "datetime"
                  },
                  "date_time": {
                    "type": "string",
                    "format": "date-time",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:\\.\\d+)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
                    "description": "ISO 8601 date-time with UTC or an explicit offset."
                  },
                  "timezone": {
                    "description": "IANA timezone; defaults to UTC when omitted.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date_time"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "date"
                  },
                  "date": {
                    "type": "string",
                    "format": "date",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))$",
                    "description": "ISO 8601 date for an all-day event."
                  },
                  "timezone": {
                    "description": "IANA timezone for the date.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date"
                ],
                "additionalProperties": false
              }
            ]
          },
          "end": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "datetime"
                  },
                  "date_time": {
                    "type": "string",
                    "format": "date-time",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:\\.\\d+)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
                    "description": "ISO 8601 date-time with UTC or an explicit offset."
                  },
                  "timezone": {
                    "description": "IANA timezone; defaults to UTC when omitted.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date_time"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "date"
                  },
                  "date": {
                    "type": "string",
                    "format": "date",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))$",
                    "description": "ISO 8601 date for an all-day event."
                  },
                  "timezone": {
                    "description": "IANA timezone for the date.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date"
                ],
                "additionalProperties": false
              }
            ]
          },
          "timezone": {
            "description": "IANA timezone for the event.",
            "type": "string"
          },
          "notify": {
            "description": "Google-only guest update policy; omit for Microsoft.",
            "type": "string",
            "enum": [
              "all",
              "externalOnly",
              "none"
            ]
          }
        },
        "required": [
          "calendar_id",
          "title",
          "attendees",
          "start",
          "end"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_get_event",
      "description": "Get one event using both its exact parent calendar_id and event_id. Never identify an event only by title or date.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact event ID returned by calendar_list_events or calendar_create_event, paired with its parent calendar_id. Calendar event ID: Exact event id in the selected calendar/account. Obtain with: calendar_list_events -> event.id; calendar_create_event -> created event.id Never pass: event title, date."
          }
        },
        "required": [
          "calendar_id",
          "event_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_update_event",
      "description": "Update explicit fields on a resolved event. Keep event_id paired with the parent calendar_id and do not blindly retry after an ambiguous failure.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact event ID returned by calendar_list_events or calendar_create_event, paired with its parent calendar_id. Calendar event ID: Exact event id in the selected calendar/account. Obtain with: calendar_list_events -> event.id; calendar_create_event -> created event.id Never pass: event title, date."
          },
          "title": {
            "type": "string"
          },
          "body": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "background_color": {
            "type": "string"
          },
          "transparency": {
            "type": "string",
            "enum": [
              "opaque",
              "transparent"
            ]
          },
          "visibility": {
            "type": "string",
            "enum": [
              "public",
              "private"
            ]
          },
          "conference": {
            "type": "object",
            "properties": {
              "provider": {
                "type": "string",
                "enum": [
                  "google_meet",
                  "zoom",
                  "skype",
                  "teams",
                  "unknown"
                ]
              },
              "conference_id": {
                "type": "string"
              },
              "url": {
                "type": "string",
                "format": "uri"
              }
            },
            "required": [
              "provider"
            ],
            "additionalProperties": false
          },
          "is_attendees_list_hidden": {
            "type": "boolean"
          },
          "attendees": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "format": "email",
                  "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
                },
                "type": {
                  "type": "string",
                  "enum": [
                    "required",
                    "optional"
                  ]
                }
              },
              "required": [
                "email"
              ],
              "additionalProperties": false
            }
          },
          "recurrence": {
            "description": "RFC5545 RRULE, EXRULE, RDATE or EXDATE lines.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "start": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "datetime"
                  },
                  "date_time": {
                    "type": "string",
                    "format": "date-time",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:\\.\\d+)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
                    "description": "ISO 8601 date-time with UTC or an explicit offset."
                  },
                  "timezone": {
                    "description": "IANA timezone; defaults to UTC when omitted.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date_time"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "date"
                  },
                  "date": {
                    "type": "string",
                    "format": "date",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))$",
                    "description": "ISO 8601 date for an all-day event."
                  },
                  "timezone": {
                    "description": "IANA timezone for the date.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date"
                ],
                "additionalProperties": false
              }
            ]
          },
          "end": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "datetime"
                  },
                  "date_time": {
                    "type": "string",
                    "format": "date-time",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:\\.\\d+)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
                    "description": "ISO 8601 date-time with UTC or an explicit offset."
                  },
                  "timezone": {
                    "description": "IANA timezone; defaults to UTC when omitted.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date_time"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "date"
                  },
                  "date": {
                    "type": "string",
                    "format": "date",
                    "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))$",
                    "description": "ISO 8601 date for an all-day event."
                  },
                  "timezone": {
                    "description": "IANA timezone for the date.",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "date"
                ],
                "additionalProperties": false
              }
            ]
          },
          "timezone": {
            "description": "IANA timezone for the event.",
            "type": "string"
          },
          "notify": {
            "description": "Google-only guest update policy; omit for Microsoft.",
            "type": "string",
            "enum": [
              "all",
              "externalOnly",
              "none"
            ]
          }
        },
        "required": [
          "calendar_id",
          "event_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_delete_event",
      "description": "Delete one resolved calendar event. Confirm the intended event and preserve its parent calendar_id; this removes it only from the connected account's calendar.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact event ID returned by calendar_list_events or calendar_create_event, paired with its parent calendar_id. Calendar event ID: Exact event id in the selected calendar/account. Obtain with: calendar_list_events -> event.id; calendar_create_event -> created event.id Never pass: event title, date."
          }
        },
        "required": [
          "calendar_id",
          "event_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_rsvp_event",
      "description": "Accept, tentatively accept or decline an invitation for the connected account only. Resolve the event first; Outlook organizers cannot RSVP and declining can remove the event from their calendar.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact event ID returned by calendar_list_events or calendar_create_event, paired with its parent calendar_id. Calendar event ID: Exact event id in the selected calendar/account. Obtain with: calendar_list_events -> event.id; calendar_create_event -> created event.id Never pass: event title, date."
          },
          "status": {
            "type": "string",
            "enum": [
              "yes",
              "maybe",
              "no"
            ]
          },
          "comment": {
            "type": "string"
          }
        },
        "required": [
          "calendar_id",
          "event_id",
          "status"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_cancel_event",
      "description": "Cancel an event organized by the connected account and notify attendees. This differs from deleting an event only from the user's calendar; confirm the exact event before cancellation.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact event ID returned by calendar_list_events or calendar_create_event, paired with its parent calendar_id. Calendar event ID: Exact event id in the selected calendar/account. Obtain with: calendar_list_events -> event.id; calendar_create_event -> created event.id Never pass: event title, date."
          },
          "comment": {
            "description": "Cancellation message sent to attendees; supported by Outlook only.",
            "type": "string"
          }
        },
        "required": [
          "calendar_id",
          "event_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "calendar_restore_event",
      "description": "Restore an event previously cancelled by the connected organizer. Available only for Google Calendar while Google still retains the cancelled event; report the provider error for Outlook without substituting another action.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "calendar_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact calendar ID returned by calendar_list_calendars; never pass a calendar name. Calendar ID: Exact Google/Microsoft calendar id. Obtain with: calendar_list -> calendar.id Never pass: calendar display name."
          },
          "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact event ID returned by calendar_list_events or calendar_create_event, paired with its parent calendar_id. Calendar event ID: Exact event id in the selected calendar/account. Obtain with: calendar_list_events -> event.id; calendar_create_event -> created event.id Never pass: event title, date."
          }
        },
        "required": [
          "calendar_id",
          "event_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "agent_id_guide",
      "description": "Return the authoritative Nilyo ID semantics guide. Use this when unsure whether a tool needs a LinkedIn system user ID, public identifier, request ID, post/comment/chat/message ID, inbox/contract ID, email/thread/folder/attachment ID, calendar/event ID or recruiting/list ID. The guide explains exactly which previous tool produces each ID and which lookalike values must NOT be passed.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id_type": {
            "type": "string",
            "enum": [
              "linkedin_user_id",
              "linkedin_public_identifier",
              "linkedin_company_id",
              "relation_request_id",
              "post_id",
              "comment_id",
              "chat_id",
              "message_id",
              "inbox_id",
              "contract_id",
              "job_id",
              "email_id",
              "email_reply_id",
              "thread_id",
              "folder_id",
              "attachment_id",
              "calendar_id",
              "event_id",
              "recruiter_project_id",
              "applicant_id",
              "sales_nav_list_id"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_get_profile_from_url",
      "description": "Resolve a full LinkedIn person profile URL into the real LinkedIn profile and stable system user ID required by downstream actions. Example: https://www.linkedin.com/in/john-doe-123/?trk=foo -> extract ONLY 'john-doe-123' from the /in/{public_identifier}/ pathname, ignore query parameters/fragments/trailing slash, call Get User Profile, and return the profile including result.id. Use result.id for connection requests, new conversations and other actions requiring user_id. Never pass the full URL to those action tools. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID.\nLinkedIn public identifier: Slug after /in/ in a LinkedIn profile URL. Example https://www.linkedin.com/in/john-doe/?trk=x -> john-doe. Obtain with: extract only pathname segment immediately after /in/; ignore query string, fragment and trailing slash Never pass: full URL, ACo system ID when a public identifier is specifically required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "profile_url": {
            "type": "string",
            "format": "uri",
            "description": "Full linkedin.com/in/... person profile URL. Query parameters are ignored."
          },
          "with_sections": {
            "description": "Optional LinkedIn Classic profile sections such as linkedin_experience, linkedin_education or linkedin_skills.",
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "linkedin_*",
                "linkedin_experience",
                "linkedin_education",
                "linkedin_languages",
                "linkedin_skills",
                "linkedin_certifications",
                "linkedin_volunteer_experience",
                "linkedin_projects",
                "linkedin_recommendations",
                "linkedin_recruiting_activity",
                "linkedin_interests",
                "linkedin_*_preview",
                "linkedin_experience_preview",
                "linkedin_education_preview",
                "linkedin_languages_preview",
                "linkedin_skills_preview",
                "linkedin_certifications_preview",
                "linkedin_volunteer_experience_preview",
                "linkedin_projects_preview",
                "linkedin_recommendations_preview",
                "linkedin_recruiting_activity_preview",
                "linkedin_interests_preview"
              ]
            }
          }
        },
        "required": [
          "profile_url"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_resolve_person",
      "description": "Resolve a human description (name, role, employer, location keywords) to LinkedIn people and their stable system IDs before an action. Use when the user says 'message Sarah at Acme' or 'connect with the CTO of X' rather than providing an ID. If multiple plausible results remain, show/disambiguate them before any write. LinkedIn system user ID: Stable LinkedIn/Unipile user id, commonly ACo... for Classic profiles. Obtain with: linkedin_get_profile_from_url(profile_url) -> result.id; linkedin_get_profile(user_id_or_public_identifier) -> result.id; linkedin_search_people(...) -> selected result.id Never pass: full linkedin.com/in/... URL, person name, company ID.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "keywords": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50
          },
          "filters": {
            "description": "Optional already-resolved LinkedIn search filters. If a filter needs a provider parameter ID, resolve it first with linkedin_get_search_parameters.",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "keywords"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_resolve_company",
      "description": "Resolve a human company name to LinkedIn company results and exact company IDs. Use before employee searches, company profile reads or company mentions. LinkedIn company mentions require the numeric company ID; the slug from /company/google/ is not sufficient. LinkedIn company ID: Provider numeric/company ID. For mentions LinkedIn requires numeric company ID, not the company URL slug. Obtain with: linkedin_search_companies -> selected result.id; linkedin_get_company after resolving a company Never pass: linkedin.com/company/google URL, company slug such as google, company name.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "keywords": {
            "type": "string",
            "minLength": 1
          },
          "filters": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "keywords"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_resolve_my_post",
      "description": "Resolve a user's description such as 'my latest post' into exact post IDs before comments/reactions/edit/delete. Returns the account owner's posts; choose the matching post and reuse result.id. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "social_resolve_comment",
      "description": "List comments for an already-resolved post so the agent can map a human reference such as 'the comment from Sarah' to exact comment_id before replying/reacting/editing/deleting. First resolve post_id. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it.\nComment ID: Exact comment id scoped to the identified post. Obtain with: linkedin_list_post_comments(post_id) -> comment.id; instagram_list_post_comments(post_id) -> comment.id Never pass: comment text, post_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "post_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact provider post ID returned by post search/list/get. Never pass post text, author ID or a post URL. Post ID: Exact provider/Unipile post id; LinkedIn commonly uses a preformatted/base64-style post ID. Obtain with: linkedin_search_posts -> result.id; linkedin_list_user_posts -> result.id; instagram_list_user_posts -> result.id Never pass: post text, author user ID, URL unless a resolver tool explicitly accepts it."
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_resolve_chat",
      "description": "List/search conversations so a human recipient/topic can be mapped to exact chat_id before reading/replying or resolving message_id. Do not use a person name as chat_id. For LinkedIn use linkedin_list_inboxes then linkedin_list_inbox_chats because account-wide chat listing is not supported. Chat ID: Exact provider chat/conversation ID. LinkedIn chat IDs may also be visible in /messaging/thread/{chat_id}/ URLs. Obtain with: provider list conversations/inbox chats -> chat.id Never pass: person name, user_id, message_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "provider": {
            "type": "string",
            "enum": [
              "whatsapp",
              "instagram",
              "telegram"
            ]
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "cursor": {
            "type": "string"
          },
          "is_unread": {
            "type": "boolean"
          }
        },
        "required": [
          "provider"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "email_resolve_message",
      "description": "Search/list email candidates so a human reference such as sender, recipient, subject or date can be mapped to exact email_id before read/reply/trash/move/attachment operations. For generic IMAP, if account-wide listing is unavailable, use email_list_folders then email_list_folder_messages instead. Email provider ID: Exact email ID returned by Nilyo/Unipile for the connected mailbox. On IMAP (and Exchange over IMAP) the ID encodes folder + UID: it CHANGES when the email is moved or a draft is updated; always reuse the id returned by the last operation. Obtain with: email_list_messages or email_list_folder_messages -> email.id; the id returned by email_move_or_label / email_update_draft after a change Never pass: RFC Message-ID unless a tool explicitly documents it, subject, an email_id captured before a move/update on IMAP.\nEmail reply reference: For Gmail/Outlook normally use the provider email id. For generic IMAP, reply_to_message_id may be the RFC822 Message-ID returned in the email data. Obtain with: email_read_message -> inspect id and provider/RFC Message-ID fields Never pass: subject, sender email address.\nEmail thread ID: Exact thread/conversation id returned by the email provider when available. Obtain with: email list/read result -> thread_id Never pass: email_id unless equal according to returned data.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "cursor": {
            "type": "string"
          },
          "before": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:\\.\\d+)?(?:Z))$"
          },
          "after": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:\\.\\d+)?(?:Z))$"
          },
          "from": {
            "type": "string"
          },
          "to": {
            "type": "string"
          },
          "any_email": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "email_resolve_folder",
      "description": "List mailbox folders/labels to map human names such as Inbox, Archive, Junk or a custom folder to exact folder_id before moving/labeling or listing folder messages. This is especially important for IMAP. Mailbox folder/label ID: Exact Gmail label, Outlook folder or IMAP folder id. Obtain with: email_list_folders -> folder.id Never pass: human folder name such as Inbox when an ID is required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 250
          },
          "cursor": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_classic_list_job_applicants",
      "description": "List applicants for a LinkedIn Classic job owned by the connected account. Resolve the exact Classic job_id first. Filters are passed as V2 applicant filters (for example ratings/experience) and must use provider enums/IDs rather than guessed display values.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "job_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn job posting ID. Classic: obtain from owned Classic job postings. Recruiter: obtain from the selected Recruiter project's jobs. Never use title/URL as job_id."
          },
          "filters": {
            "description": "Optional V2 Classic applicant filters. Use documented enum/filter IDs; omit unknown filters rather than guessing.",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "job_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_classic_get_job_applicant",
      "description": "Get one applicant to a LinkedIn Classic job. Required chain: owned Classic job -> job_id -> list applicants -> applicant.id -> this tool.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "job_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn job posting ID. Classic: obtain from owned Classic job postings. Recruiter: obtain from the selected Recruiter project's jobs. Never use title/URL as job_id."
          },
          "applicant_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact applicant profile ID returned by the corresponding applicants listing. Keep it paired with the Classic job_id or Recruiter project_id that produced it."
          }
        },
        "required": [
          "job_id",
          "applicant_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_classic_get_applicant_resume",
      "description": "Download/retrieve the resume of one applicant to a LinkedIn Classic job. Never infer applicant_id from name; list applicants first and reuse exact applicant.id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "job_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn job posting ID. Classic: obtain from owned Classic job postings. Recruiter: obtain from the selected Recruiter project's jobs. Never use title/URL as job_id."
          },
          "applicant_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact applicant profile ID returned by the corresponding applicants listing. Keep it paired with the Classic job_id or Recruiter project_id that produced it."
          }
        },
        "required": [
          "job_id",
          "applicant_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_recruiter_list_applicants",
      "description": "List applicants from a LinkedIn Recruiter Hiring Project talent pool. Resolve project_id first. Recruiter applicants are project-scoped; never substitute a Classic job_id for project_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "project_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn Recruiter Hiring Project ID from linkedin_recruiter_list_projects. Never pass project name."
          },
          "cursor": {
            "description": "Opaque next_cursor returned by previous page; never invent.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "filters": {
            "description": "Recruiter talent-pool applicant filters such as sort/seniority/current-company using documented V2 enums/parameter IDs.",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "project_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_recruiter_get_applicant",
      "description": "Get one Recruiter applicant. Required chain: list/select Hiring Project -> project.id -> list applicants -> applicant.id. applicant_id is scoped to the selected project context.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "project_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn Recruiter Hiring Project ID from linkedin_recruiter_list_projects. Never pass project name."
          },
          "applicant_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact applicant profile ID returned by the corresponding applicants listing. Keep it paired with the Classic job_id or Recruiter project_id that produced it."
          }
        },
        "required": [
          "project_id",
          "applicant_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_recruiter_get_applicant_resume",
      "description": "Retrieve a Recruiter applicant resume. Resolve exact project_id and applicant_id first; never use candidate name or LinkedIn URL as applicant_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "project_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn Recruiter Hiring Project ID from linkedin_recruiter_list_projects. Never pass project name."
          },
          "applicant_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact applicant profile ID returned by the corresponding applicants listing. Keep it paired with the Classic job_id or Recruiter project_id that produced it."
          }
        },
        "required": [
          "project_id",
          "applicant_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_verify_company_member_email",
      "description": "Start LinkedIn company-member identity verification when a job/company action fails with insufficient permissions. Resolve company_id first and submit the actual verification email. Preserve the returned challenge_id for linkedin_submit_company_member_otp.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "company_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn company ID from company search/profile resolution; never use company name or slug unless a resolver explicitly converts it."
          },
          "product": {
            "default": "classic",
            "type": "string",
            "enum": [
              "classic",
              "recruiter"
            ]
          },
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "description": "Email address to submit for company-member verification."
          }
        },
        "required": [
          "company_id",
          "product",
          "email"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "linkedin_submit_company_member_otp",
      "description": "Complete LinkedIn company-member verification. Reuse the same email and exact challenge_id returned by linkedin_verify_company_member_email, and submit only the real user-provided code; never invent or loop over codes.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "company_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact LinkedIn company ID from company search/profile resolution; never use company name or slug unless a resolver explicitly converts it."
          },
          "product": {
            "default": "classic",
            "type": "string",
            "enum": [
              "classic",
              "recruiter"
            ]
          },
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
          },
          "challenge_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact challenge_id returned by linkedin_verify_company_member_email."
          },
          "code": {
            "type": "string",
            "minLength": 3,
            "maxLength": 12,
            "description": "Actual verification code received by the user. Never invent."
          }
        },
        "required": [
          "company_id",
          "product",
          "email",
          "challenge_id",
          "code"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_send_voice_note",
      "description": "Send an audio attachment as a native/playable voice note where the connected provider supports it (notably WhatsApp/Telegram). Resolve chat_id first. Provide base64 content + exact MIME type/filename. Do not send the same voice note repeatedly after an uncertain timeout; read chat state first.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat.id resolved/listed for the destination conversation; never pass a person name/user_id here."
          },
          "content_base64": {
            "type": "string",
            "minLength": 1,
            "description": "Base64-encoded audio bytes, not a URL/path."
          },
          "content_type": {
            "type": "string",
            "minLength": 1,
            "description": "Audio MIME type. WhatsApp voice notes expect Opus in OGG: audio/ogg (codecs=opus); MP3 is sent as a regular audio file."
          },
          "filename": {
            "type": "string",
            "minLength": 1
          },
          "duration_seconds": {
            "description": "Duration of the audio in seconds when known (shown by the provider on the voice note).",
            "type": "number",
            "exclusiveMinimum": 0
          },
          "text": {
            "description": "Optional caption/text only when supported/requested.",
            "type": "string"
          }
        },
        "required": [
          "chat_id",
          "content_base64",
          "content_type",
          "filename"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "message_send_native_media",
      "description": "Send one media/file attachment using the provider's native rendering mode where supported. Resolve chat_id first. Useful for inline image/video/native media; for WhatsApp multiple attachments may become separate provider messages, so this tool intentionally sends one attachment per call for predictable behavior.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Omit when the user has one account for this provider. When several exist, Nilyo never guesses: list them (display name, identifier, provider user ID), choose the one the user named or ask, and pass its ID here.",
            "type": "string"
          },
          "chat_id": {
            "type": "string",
            "minLength": 1,
            "description": "Exact chat.id resolved/listed for the destination conversation; never pass a person name/user_id here."
          },
          "content_base64": {
            "type": "string",
            "minLength": 1
          },
          "content_type": {
            "type": "string",
            "minLength": 1
          },
          "filename": {
            "type": "string",
            "minLength": 1
          },
          "text": {
            "type": "string"
          }
        },
        "required": [
          "chat_id",
          "content_base64",
          "content_type",
          "filename"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_list_available_events",
      "description": "List realtime Unipile V2 event types Nilyo can subscribe an agent to, plus the common event envelope. Use this before creating a destination if the user's requested trigger is ambiguous.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_get_setup_guide",
      "description": "Get runtime-specific instructions for receiving Unipile realtime events in n8n, OpenClaw, Hermes, Make or a generic agent. Use BEFORE creating the webhook when the user has not yet produced a public receiver URL.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "runtime": {
            "type": "string",
            "enum": [
              "n8n",
              "openclaw",
              "hermes",
              "make",
              "generic"
            ]
          }
        },
        "required": [
          "runtime"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_create_destination",
      "description": "Create a realtime event destination for this Nilyo user. Nilyo uses its global Unipile control-plane key server-side but ALWAYS restricts the Unipile endpoint to account IDs owned by this authenticated user. By default matching future accounts are automatically added. For n8n use its Test URL first, validate with a Development/Mock event, then update to Production URL.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "runtime": {
            "type": "string",
            "enum": [
              "n8n",
              "openclaw",
              "hermes",
              "make",
              "generic"
            ]
          },
          "request_url": {
            "type": "string",
            "format": "uri",
            "description": "Public HTTPS POST receiver URL supplied by the target runtime. Never guess a URL."
          },
          "events": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "account.status.disconnected",
                "account.status.running",
                "account.status.errored",
                "account.status.degraded",
                "account.status.partial",
                "account.locked",
                "account.unlocked",
                "account.add",
                "account.reconnect",
                "account.remove",
                "account.initial_sync.running",
                "account.initial_sync.failed",
                "account.initial_sync.completed",
                "message.new",
                "message.update",
                "message.delete",
                "message.receipt.read",
                "message.receipt.delivery",
                "message.reaction.new",
                "message.reaction.delete",
                "chat.delete",
                "chat.update",
                "email.new",
                "email.new.bounce",
                "email.delete",
                "email.draft.new",
                "email.draft.delete",
                "email.folder.create",
                "email.folder.update",
                "email.folder.delete",
                "calendar.create",
                "calendar.update",
                "calendar.delete",
                "calendar.event.new",
                "calendar.event.update",
                "calendar.event.delete",
                "tracking.open",
                "tracking.click",
                "relation.new",
                "follower.new"
              ]
            },
            "description": "Exact Unipile V2 event types. Call webhook_list_available_events first when unsure. Do not invent event names."
          },
          "providers": {
            "description": "Optional provider filter. google includes Gmail and outlook includes Microsoft accounts. With auto_include_new_accounts=true, Nilyo keeps this Unipile endpoint's account_ids synchronized when the user connects matching accounts later.",
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "linkedin",
                "whatsapp",
                "instagram",
                "telegram",
                "google",
                "outlook",
                "imap"
              ]
            }
          },
          "account_ids": {
            "description": "Optional explicit Unipile account IDs returned by list_connected_accounts. Nilyo rejects the whole request if any ID is not owned by the authenticated user.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^acc_"
            }
          },
          "auto_include_new_accounts": {
            "default": true,
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "runtime",
          "request_url",
          "events",
          "auto_include_new_accounts"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_list_destinations",
      "description": "List webhook destinations created by this Nilyo user. These are control-plane records; realtime traffic flows directly Unipile -> destination, not through Nilyo.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_get_destination",
      "description": "Get one Nilyo-owned webhook destination and its configured events/provider/account selection.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "destination_id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "Nilyo webhook destination ID returned by webhook_create_destination/webhook_list_destinations. Never use the Unipile endpoint ID directly."
          }
        },
        "required": [
          "destination_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_update_destination",
      "description": "Update a webhook destination URL, events or provider/account selection. Common n8n chain: create with Test URL -> receive mock event -> update same destination to Production URL. Nilyo recomputes authorized account_ids server-side.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "destination_id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "Nilyo webhook destination ID returned by webhook_create_destination/webhook_list_destinations. Never use the Unipile endpoint ID directly."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "runtime": {
            "type": "string",
            "enum": [
              "n8n",
              "openclaw",
              "hermes",
              "make",
              "generic"
            ]
          },
          "request_url": {
            "type": "string",
            "format": "uri",
            "description": "Public HTTPS POST receiver URL supplied by the target runtime. Never guess a URL."
          },
          "events": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "account.status.disconnected",
                "account.status.running",
                "account.status.errored",
                "account.status.degraded",
                "account.status.partial",
                "account.locked",
                "account.unlocked",
                "account.add",
                "account.reconnect",
                "account.remove",
                "account.initial_sync.running",
                "account.initial_sync.failed",
                "account.initial_sync.completed",
                "message.new",
                "message.update",
                "message.delete",
                "message.receipt.read",
                "message.receipt.delivery",
                "message.reaction.new",
                "message.reaction.delete",
                "chat.delete",
                "chat.update",
                "email.new",
                "email.new.bounce",
                "email.delete",
                "email.draft.new",
                "email.draft.delete",
                "email.folder.create",
                "email.folder.update",
                "email.folder.delete",
                "calendar.create",
                "calendar.update",
                "calendar.delete",
                "calendar.event.new",
                "calendar.event.update",
                "calendar.event.delete",
                "tracking.open",
                "tracking.click",
                "relation.new",
                "follower.new"
              ]
            },
            "description": "Exact Unipile V2 event types. Call webhook_list_available_events first when unsure. Do not invent event names."
          },
          "providers": {
            "description": "Optional provider filter. google includes Gmail and outlook includes Microsoft accounts. With auto_include_new_accounts=true, Nilyo keeps this Unipile endpoint's account_ids synchronized when the user connects matching accounts later.",
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "linkedin",
                "whatsapp",
                "instagram",
                "telegram",
                "google",
                "outlook",
                "imap"
              ]
            }
          },
          "account_ids": {
            "description": "Optional explicit Unipile account IDs returned by list_connected_accounts. Nilyo rejects the whole request if any ID is not owned by the authenticated user.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^acc_"
            }
          },
          "auto_include_new_accounts": {
            "type": "boolean"
          },
          "enabled": {
            "type": "boolean"
          }
        },
        "required": [
          "destination_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_delete_destination",
      "description": "Delete an Nilyo-owned Unipile webhook endpoint. This stops future realtime delivery to that destination; require explicit user intent.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "destination_id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "Nilyo webhook destination ID returned by webhook_create_destination/webhook_list_destinations. Never use the Unipile endpoint ID directly."
          }
        },
        "required": [
          "destination_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_get_delivery_logs",
      "description": "Inspect recent Unipile webhook delivery conversations for this destination. Use when an agent/n8n receiver did not wake or a test event was not observed. This is preferred to guessing whether delivery occurred.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "destination_id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "Nilyo webhook destination ID returned by webhook_create_destination/webhook_list_destinations. Never use the Unipile endpoint ID directly."
          },
          "limit": {
            "default": 20,
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          }
        },
        "required": [
          "destination_id",
          "limit"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "webhook_test_destination",
      "description": "Return the exact test procedure for a destination. In a Unipile Development Application, use Mock accounts/Test & Debug to generate a real fake event, then inspect webhook_get_delivery_logs and the target runtime. Nilyo does not fabricate provider event payloads because the Unipile mock event is the source of truth.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "destination_id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "Nilyo webhook destination ID returned by webhook_create_destination/webhook_list_destinations. Never use the Unipile endpoint ID directly."
          }
        },
        "required": [
          "destination_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "account_connect",
      "description": "Get a secure connection (or reconnection) link for one of the user's provider accounts: LinkedIn, WhatsApp, Instagram, Telegram, Google/Gmail, Microsoft/Outlook or generic IMAP email. Use when the user asks to connect/add/reconnect an account, or after a connect_account/reconnect_account result. Pass account_id only to re-authenticate an existing disconnected account (from list_connected_accounts). For WhatsApp and Telegram prefer whatsapp_connect / telegram_connect, which show the QR code directly in the conversation; this tool is the browser fallback. Never ask the user for provider passwords; the link opens the provider's own authentication.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "linkedin",
              "whatsapp",
              "instagram",
              "telegram",
              "google",
              "outlook",
              "imap",
              "email"
            ],
            "description": "Provider to connect. Use 'email' to let the user pick Gmail, Outlook or IMAP; 'imap' for a generic mailbox."
          },
          "account_id": {
            "description": "Nilyo connection ID (unipile_account_id from list_connected_accounts) of an EXISTING account to re-authenticate. Omit to connect a new account.",
            "type": "string"
          }
        },
        "required": [
          "provider"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "whatsapp_connect",
      "description": "Connect or reconnect the user's WhatsApp account directly in the conversation without leaving the agent. Default mode returns a QR code image to scan from WhatsApp > Linked devices; with phone_number it returns a pairing code to type on the phone instead (useful when the QR image cannot be displayed). Then poll account_connection_status until connected. Requires an active trial/subscription and a free WhatsApp slot on the plan; otherwise the result explains the upgrade. Use account_id to re-authenticate an existing disconnected WhatsApp account.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Nilyo connection ID (unipile_account_id from list_connected_accounts) of an EXISTING account to re-authenticate. Omit to connect a new account.",
            "type": "string"
          },
          "phone_number": {
            "description": "Optional. International phone number of the WhatsApp account, digits only (e.g. 33612345678). When provided, a pairing code is returned instead of a QR code.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "telegram_connect",
      "description": "Connect or reconnect the user's Telegram account directly in the conversation: returns a QR code image to scan from Telegram > Settings > Devices > Link Desktop Device, then poll account_connection_status until connected. Accounts protected by a Telegram cloud password (2FA) must use the browser link returned by account_connect instead. Use account_id to re-authenticate an existing disconnected Telegram account.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "account_id": {
            "description": "Nilyo connection ID (unipile_account_id from list_connected_accounts) of an EXISTING account to re-authenticate. Omit to connect a new account.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "imap_connect",
      "description": "Connect a generic IMAP/SMTP mailbox directly from the conversation with the mailbox credentials (alternative to the account_connect browser link, which is preferred when the user does not want to type a password in the chat). Nilyo forwards the credentials once to the connection service and never stores them. Start with user + password only (servers are auto-detected); if the result says the IMAP or SMTP configuration is invalid, ask the user for the servers (e.g. OVH: imap ssl0.ovh.net:993 ssl, smtp ssl0.ovh.net:465 ssl; Gmail/Microsoft mailboxes should use the Google/Outlook connection instead). Use account_id to re-authenticate an existing disconnected IMAP account.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "user": {
            "type": "string",
            "minLength": 1,
            "description": "Mailbox login, usually the full email address."
          },
          "password": {
            "type": "string",
            "minLength": 1,
            "description": "Mailbox password or app password, exactly as given by the user."
          },
          "imap_host": {
            "type": "string"
          },
          "imap_port": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "imap_encryption": {
            "type": "string",
            "enum": [
              "tls",
              "ssl",
              "starttls",
              "default"
            ]
          },
          "smtp_host": {
            "type": "string"
          },
          "smtp_port": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "smtp_encryption": {
            "type": "string",
            "enum": [
              "tls",
              "ssl",
              "starttls",
              "default"
            ]
          },
          "initial_sync": {
            "description": "Enable the initial mailbox synchronization so account-wide listing/search (email_list_messages, email_resolve_message) works; without it, use email_list_folders + email_list_folder_messages. Default false (faster connection).",
            "type": "boolean"
          },
          "account_id": {
            "description": "Nilyo connection ID (unipile_account_id from list_connected_accounts) of an EXISTING account to re-authenticate. Omit to connect a new account.",
            "type": "string"
          }
        },
        "required": [
          "user",
          "password"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "account_connection_status",
      "description": "Check an in-conversation WhatsApp/Telegram connection started with whatsapp_connect or telegram_connect. Returns pending (with the current QR code image or pairing code, refreshed when it changed), connected, expired or failed. Poll every 10-20 seconds until the status is connected, then retry the user's original request. Omit intent_id to check the most recent attempt.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "intent_id": {
            "description": "intent_id returned by whatsapp_connect/telegram_connect. Omit for the latest attempt.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "account_list_attention",
      "description": "List the user's connected accounts that currently need re-authentication (status disconnected) so the agent can proactively offer to reconnect them before a request fails. Returns an empty list when everything is healthy.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "account_get_subscription",
      "description": "Show the user's Nilyo plan, trial/subscription status, account limits, team seats and members, plus the available billing actions (subscribe, upgrade, add seats, invite). Use when the user asks about their plan, trial, price, seats, team, why an action is blocked, or before proposing an upgrade. Never exposes payment details.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "account_start_subscription",
      "description": "Return Stripe Checkout links (one-click hosted payment, no need to visit the website) to start or restore the paid subscription when the user is on trial, the trial expired or the subscription lapsed. Use after a subscribe result or when the user asks to subscribe/pay/upgrade before having a subscription. If the user already has an active Stripe subscription, use account_change_plan instead. Present monthly and annual options; default to the current plan unless the user chose another one.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "plan": {
            "type": "string",
            "enum": [
              "personal",
              "personal_plus",
              "team"
            ],
            "description": "Target plan: personal (1 account per provider), personal_plus (up to 10 accounts, several per provider allowed) or team (5 isolated users, extra seats available)."
          },
          "interval": {
            "description": "Billing interval. Annual = 10 monthly payments (2 months free). Defaults to the user's current interval.",
            "type": "string",
            "enum": [
              "month",
              "year"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "account_change_plan",
      "description": "Change the user's plan (Personal, Personal Plus, Team) or billing interval. During the trial the choice is free and applies immediately. With an active Stripe subscription the change is prorated immediately on the saved payment method: FIRST call without confirm to get the price and the explanation (and a one-click Stripe confirmation link), show them to the user, and only call again with confirm=true after explicit approval. Use when a second account of a provider needs Personal Plus, when the user wants Team for colleagues, or asks to switch monthly/annual.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "plan": {
            "type": "string",
            "enum": [
              "personal",
              "personal_plus",
              "team"
            ],
            "description": "Target plan: personal (1 account per provider), personal_plus (up to 10 accounts, several per provider allowed) or team (5 isolated users, extra seats available)."
          },
          "interval": {
            "description": "Billing interval. Annual = 10 monthly payments (2 months free). Defaults to the user's current interval.",
            "type": "string",
            "enum": [
              "month",
              "year"
            ]
          },
          "confirm": {
            "description": "Set true ONLY after the user explicitly confirmed the price shown to them. Without it, the tool returns the price, the proration explanation and a Stripe confirmation link instead of changing anything.",
            "type": "boolean"
          },
          "organization_name": {
            "description": "Team name when moving to the Team plan (defaults to 'My team').",
            "type": "string"
          }
        },
        "required": [
          "plan"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "team_invite_member",
      "description": "Invite a colleague to the user's Team workspace by email. Each member gets a separate login, isolated provider Scope and their own connected accounts; the team only centralizes billing and seats. Only team owners/admins can invite. When no seat is free, the result explains how to add a paid seat (team_add_seats). If the user is not on Team, propose account_change_plan(plan='team').",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "description": "Email address of the person to invite."
          }
        },
        "required": [
          "email"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "team_add_seats",
      "description": "Add (or reduce) paid extra seats on the Team subscription so more colleagues can be invited. Billing owner only; prorated immediately on the saved payment method. FIRST call without confirm to get the price and a one-click Stripe confirmation link, then call with confirm=true after the user approves. extra_seats is the ADDED number of seats relative to the current paid extra seats (use 1 to add one seat).",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "extra_seats": {
            "default": 1,
            "description": "Seats to add (positive) or remove (negative, only unused seats).",
            "type": "integer",
            "minimum": -500,
            "maximum": 500
          },
          "confirm": {
            "description": "Set true ONLY after the user explicitly confirmed the price shown to them. Without it, the tool returns the price, the proration explanation and a Stripe confirmation link instead of changing anything.",
            "type": "boolean"
          }
        },
        "required": [
          "extra_seats"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_resolve_recipient",
      "description": "Turn a person's name, nickname, phone number or handle into the exact WhatsApp/Instagram/Telegram recipient BEFORE sending: searches the user's 1-to-1 chats (ranked by most recent exchange) and contacts. Returns resolved (one clear match: send immediately with the recommended next tool, no extra confirmation needed when the user already gave the message), ambiguous (several people: ask which one, most recent conversation first) or not_found. Use for 'send a WhatsApp to Julien', 'reply to Marie on Instagram', 'message +33 6\u2026'. Never pass a name as chat_id or user_id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "whatsapp",
              "instagram",
              "telegram"
            ],
            "description": "Messaging provider named by the user. LinkedIn recipients use linkedin_resolve_person + linkedin_start_conversation instead."
          },
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Required when the user has several accounts for the provider: pick the one the user named.",
            "type": "string"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "How the user refers to the person: first name, full name, phone number or handle."
          }
        },
        "required": [
          "provider",
          "name"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "messaging_send_to_contact",
      "description": "One-step send for voice-style requests: 'send a WhatsApp to Julien saying I'm 10 minutes late'. Resolves the recipient like messaging_resolve_recipient and sends the text ONLY when exactly one person matches (existing conversation or contact). When several people match, nothing is sent and the candidates are returned so you can ask the user which one. The user must have stated the message content; do not invent it.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "whatsapp",
              "instagram",
              "telegram"
            ],
            "description": "Messaging provider named by the user. LinkedIn recipients use linkedin_resolve_person + linkedin_start_conversation instead."
          },
          "account_id": {
            "description": "Optional Nilyo connection ID (unipile_account_id from list_connected_accounts). Required when the user has several accounts for the provider: pick the one the user named.",
            "type": "string"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "How the user refers to the recipient."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "Exact message text the user asked to send."
          },
          "recipient_user_id": {
            "description": "Exact provider user ID chosen by the user after an ambiguous result; skips name matching.",
            "type": "string"
          }
        },
        "required": [
          "provider",
          "name",
          "text"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "feedback_report_bug",
      "description": "Report a bug to the Nilyo team as a tracked issue. Use after a tool failed unexpectedly (TOOL_FAILED, wrong or empty result, broken flow) and the user agrees to report it; also when the user says '\u00e7a ne marche pas' / 'report this'. Include the exact error code and details from the failing result so engineers can reproduce. Never include credentials, message bodies or third-party personal data. Ask the user for consent first (user_consent=true).",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "minLength": 8,
            "maxLength": 120,
            "description": "Short, specific title in English (what fails or what is wanted), e.g. 'email_list_messages fails on OVH IMAP mailbox with 501'."
          },
          "description": {
            "type": "string",
            "minLength": 20,
            "maxLength": 4000,
            "description": "What the user was trying to do, in their words plus your observations. No passwords, tokens, message contents or third-party personal data."
          },
          "provider": {
            "description": "Provider involved when relevant: linkedin, whatsapp, instagram, telegram, gmail, outlook, imap, calendar.",
            "type": "string"
          },
          "tool": {
            "description": "Nilyo tool name involved, if any.",
            "type": "string"
          },
          "user_consent": {
            "type": "boolean",
            "const": true,
            "description": "Must be true: the user explicitly agreed to send this report to the Nilyo team."
          },
          "steps": {
            "description": "Minimal steps to reproduce: which tools/arguments in which order (IDs can be included).",
            "type": "string",
            "maxLength": 2000
          },
          "expected": {
            "type": "string",
            "maxLength": 1000
          },
          "actual": {
            "type": "string",
            "maxLength": 1000
          },
          "error_code": {
            "description": "Error code returned by Nilyo, e.g. TOOL_FAILED, PROVIDER_VALIDATION_ERROR.",
            "type": "string"
          },
          "error_details": {
            "description": "The 'Details:' text of the failing result, verbatim.",
            "type": "string",
            "maxLength": 3000
          },
          "sentry_event_id": {
            "description": "error.sentry_event_id from the failing result, when present.",
            "type": "string",
            "maxLength": 64
          },
          "severity": {
            "description": "high = user blocked with no workaround; medium = wrong behaviour with a workaround; low = cosmetic.",
            "type": "string",
            "enum": [
              "low",
              "medium",
              "high"
            ]
          },
          "suspected_layer": {
            "description": "Where you think the bug is: 'nilyo' (tool logic, resolution, errors, billing, connection flow) or 'unipile_api' (the provider connection layer returned an error like UNIPILE_5xx/provider/\u2026). 'unknown' lets the server infer it.",
            "type": "string",
            "enum": [
              "nilyo",
              "unipile_api",
              "unknown"
            ]
          }
        },
        "required": [
          "title",
          "description",
          "user_consent"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "feedback_request_feature",
      "description": "Send a feature request or improvement idea to the Nilyo team as a tracked issue: a missing action, provider or capability, a workflow that needs too many steps, a confusing result. Use when the user asks for something Nilyo cannot do yet, or explicitly wants to suggest an improvement, and agrees to send it. Describe the user's goal and why current tools fall short. Ask the user for consent first (user_consent=true).",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "minLength": 8,
            "maxLength": 120,
            "description": "Short, specific title in English (what fails or what is wanted), e.g. 'email_list_messages fails on OVH IMAP mailbox with 501'."
          },
          "description": {
            "type": "string",
            "minLength": 20,
            "maxLength": 4000,
            "description": "What the user was trying to do, in their words plus your observations. No passwords, tokens, message contents or third-party personal data."
          },
          "provider": {
            "description": "Provider involved when relevant: linkedin, whatsapp, instagram, telegram, gmail, outlook, imap, calendar.",
            "type": "string"
          },
          "tool": {
            "description": "Nilyo tool name involved, if any.",
            "type": "string"
          },
          "user_consent": {
            "type": "boolean",
            "const": true,
            "description": "Must be true: the user explicitly agreed to send this report to the Nilyo team."
          },
          "use_case": {
            "description": "Concrete scenario the user wanted to achieve and how often it happens.",
            "type": "string",
            "maxLength": 1500
          },
          "workaround": {
            "description": "What the user does today instead, if anything.",
            "type": "string",
            "maxLength": 800
          }
        },
        "required": [
          "title",
          "description",
          "user_consent"
        ],
        "additionalProperties": false
      }
    }
  ],
  "resources": [
    {
      "uri": "ui://nilyo/action-card.html",
      "name": "Nilyo card",
      "mimeType": "text/html"
    },
    {
      "uri": "nilyo://agent-guide",
      "name": "Agent guide",
      "mimeType": "text/markdown"
    },
    {
      "uri": "nilyo://capabilities",
      "name": "Capabilities",
      "mimeType": "application/json"
    },
    {
      "uri": "nilyo://id-semantics",
      "name": "ID semantics",
      "mimeType": "application/json"
    }
  ],
  "prompts": [
    {
      "name": "linkedin_profile_to_message",
      "description": "Resolve URL to stable system ID before messaging.",
      "arguments": [
        {
          "name": "profile_url",
          "required": true
        },
        {
          "name": "message",
          "required": true
        }
      ]
    },
    {
      "name": "linkedin_applicant",
      "description": "Keep Classic job IDs and Recruiter project IDs in the correct namespace.",
      "arguments": [
        {
          "name": "request",
          "required": true
        }
      ]
    },
    {
      "name": "realtime_agent",
      "description": "Create a direct Unipile-to-agent realtime destination without AccountBridge relaying events.",
      "arguments": [
        {
          "name": "runtime",
          "required": true
        },
        {
          "name": "trigger",
          "required": true
        }
      ]
    }
  ]
}